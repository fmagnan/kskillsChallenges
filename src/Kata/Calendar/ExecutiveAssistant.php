<?php

namespace Kata\Calendar;

class ExecutiveAssistant
{

    public function computeMaxEventsToBeIn(array $events = array())
    {
        $max = 0;
        foreach ($events as $index => $reference) {
            foreach ($events as $position => $event) {
                /*
                 * to think and to implement
                 */
            }
        }
        return $max;
    }

}